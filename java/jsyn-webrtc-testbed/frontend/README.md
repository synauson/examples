# JSyn WebRTC Testbed — Frontend

React + TypeScript + Tailwind SPA. Built by Vite into `../src/main/resources/static/` where Spring Boot serves it from `classpath:/static/`.

For build instructions and architecture overview, see the root `README.md` in `examples/jsyn-webrtc-testbed/`.
