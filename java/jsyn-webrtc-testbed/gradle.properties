# Nexus credentials for pulling JSyn snapshots.
# Override with env vars NEXUS_USER / NEXUS_PASSWORD in CI.
nexusUser=
nexusPassword=

# Sensible JVM args for local Gradle.
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
